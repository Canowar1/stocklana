/** Product name. The on-chain crate and program id stay `stocklana`. */
export const APP_NAME = "Thales";

export const THEME_STORAGE_KEY = "thales-theme";
/** Previous key, still read so a stored preference survives the rename. */
export const LEGACY_THEME_STORAGE_KEY = "stocklana-theme";
